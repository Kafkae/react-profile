import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  display: block;
  width: 100%;
`;

const Add = () => (
  <Wrapper>
    <p>Add</p>
  </Wrapper>
);

export default Add;
